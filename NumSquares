class Solution {
public:
    int numSquares(int n) {
        int dp[0]=0;
        for(int i=0;i<=n;i++){
            for(int j=1;j*j<=i;j++){
               dp[i]=min(dp[i],dp(i,i-j*j));
            }
        }
        return dp[n];
    }
};
